# filterr
Image filtering with pure CSS3 filters.

List of CSS3 filters:
	filter: blur(px);
	filter: brightness(%);
	filter: contrast(%);
	filter: grayscale(%);
	filter: hue-rotate(deg);
	filter: invert(%);
	filter: opacity(%);
	filter: saturate(%);
	filter: sepia(%);

WIP
